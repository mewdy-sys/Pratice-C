Х	„то такое поток? 
	Ќаименьша€ единица обработки, котора€ может быть запущена и выполнена независимо от других потоков.
Х	 акой класс нужно использовать дл€ создани€ экземпл€ра потока? 
	System.Threading.Thread
Х	 акой делегат нужно использовать дл€ передачи метода в поток? 
	System.Threading.ThreadStart
Х	 аким способом можно передать в поток параметры? 
	делегат ParameterizedThreadStart, принимает параметр типа object.
Х	„то такое критическа€ секци€? 
	это участок кода, который должен выполн€тьс€ только одним потоком в любой момент времени
Х	„то такое раздел€емый ресурс? 
	это ресурс, к которому могут обращатьс€ несколько потоков одновременно
Х	„то такое объект синхронизации доступа к раздел€емому ресурсу? 
	это объект, который позвол€ет синхронизировать доступ к раздел€емому ресурсу. (lock, Monitor, Mutex, Semaphore и AutoResetEvent)
Х	„ем отличаетс€ использование оператора lock от класса Monitor? 
	lock: упрощенна€ оболочка вокруг Monitor. автоматически освобождает блокировку, даже если внутри блока lock происходит исключение.
	Monitor: более гибкий, €вно захватывать и освобождать блокировки.