Х	„то такое коллекци€? 
	набор объектов, управл€емых как единое целое. ћетоды дл€ добавлени€, удалени€, управлени€ и перебора.
Х	ѕеречислите члены интерфейса IEnumerable. 
	GetEnumerator() - возвращает перечислитель, который выполн€ет перебор элементов коллекции.
Х	ѕеречислите члены интерфейса IEnumerator. 
	Current { get; } - возвращает текущий элемент коллекции.
	bool MoveNext() - перемещает перечислитель к следующему элементу коллекции.
	void Reset() - устанавливает перечислитель в его начальное положение, т. е. перед первым элементом коллекции.
Х	„то такое оператор yield? 
	позвол€ет возвращать элементы последовательности по одному, не блокиру€ поток выполнени€ метода.
Х	ѕеречислите члены интерфейса IEnumerable<T>. 
	GetEnumerator() - возвращает перечислитель, который выполн€ет перебор элементов коллекции.
Х	ѕеречислите члены интерфейса IEnumerator<T>. 
	Current { get; } - возвращает текущий элемент коллекции.
	bool MoveNext() - перемещает перечислитель к следующему элементу коллекции.
	void Reset() - устанавливает перечислитель в его начальное положение, т. е. перед первым элементом коллекции.
Х	ќпишите алгоритм работы цикла foreach. 
	ѕолучает перечислитель коллекции и выполн€ет перебор элементов коллекции.
Х	ћожно ли использовать оператор yield в анонимных методах? 
	ћожет быть использован только в методах, определенных €вно.
